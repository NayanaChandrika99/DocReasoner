apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: treestore-data
  namespace: treestore
  labels:
    app: treestore
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard  # Adjust based on your cluster
